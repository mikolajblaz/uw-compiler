int main() {
  string x, y, z = "arb";
  printString(z);
  y = readString();
  printInt(readInt());
  readInt();
  printInt(readInt());
  readInt();
  printInt(readInt());

  x = readString();

  printString(x);
  printString(y);

  printInt(readInt());
  return 0;
}
